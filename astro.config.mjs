import { defineConfig } from 'astro/config';

export default defineConfig({
  base: '/https://github.com/zweifelna/personnal-website/',  // Remplace 'ton-repo' par le nom de ton dépôt GitHub
  site: 'https://zweifelna.github.io/personnal-website/',  // URL complète vers ton GitHub Pages
});
